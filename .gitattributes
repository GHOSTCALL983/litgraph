# Normalise line endings in the repository; check out native on each platform.
* text=auto eol=lf

# Windows launcher must keep CRLF to run correctly.
*.bat text eol=crlf

# Binary assets — never diff or convert.
*.pdf binary
*.png binary
*.jpg binary
*.ico binary
